export enum UserTypes {
  ADMIN,
  MANAGER,
  CUSTOMER,
}

export enum PaymentStatuses {
  UNPAID,
  PAID,
}

export enum OrderStatuses {
  ORDERED,
  CANCELED,
  DELIVERED_SUCCESSFULLY,
}

export enum Actions {
  CREATE,
  UPDATE,
  DELETE,
}
